version: "3.9"
services:
  alphaengine:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./.cache:/app/.cache
      - ./logs:/app/logs
      - ./.env:/app/.env
    restart: unless-stopped
